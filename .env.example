# Database
POSTGRES_USER=newsflow
POSTGRES_PASSWORD=newsflow_dev_password
POSTGRES_DB=newsflow
DATABASE_URL=postgresql://newsflow:newsflow_dev_password@postgres:5432/newsflow

# Backend
NODE_ENV=development
PORT=3000
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# OpenAI (Optional - for AI features)
OPENAI_API_KEY=

# AWS (Optional - for production deployment)
AWS_REGION=ap-southeast-2
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# Frontend
VITE_API_URL=http://localhost:3000

# Mobile App API URL
# 設定方式：
# 1. Mac: 執行 `ipconfig getifaddr en0` 取得本機 IP
# 2. Windows: 執行 `ipconfig` 找到 IPv4 位址
# 3. 將下方的 YOUR_LOCAL_IP 替換成你的 IP
# 4. 如果使用 iOS 模擬器且 backend 在本機，可以使用 http://localhost:3000
# 5. 如果使用實體設備或 Android 模擬器，必須使用實際 IP
# 範例: EXPO_PUBLIC_API_URL=http://10.0.0.160:3000
EXPO_PUBLIC_API_URL=http://localhost:3000
